<% content_for :main_picture do %>
  <%= image_tag "survey/#{@question_picture}"  %>
  <p class="image_title"><%= @question_title %></p>
  <p class="image_title"><%= "#{@current_question} de #{@total_questions}" %></p>
<% end %>
